# ── Servidor ───────────────────────────────────────────────
PORT=4000
# Origem do frontend (CORS). Em dev: http://localhost:5173
CLIENT_ORIGIN=http://localhost:5173

# ── Base de dados ──────────────────────────────────────────
# Local: postgres://postgres:postgres@localhost:5432/innov
# Neon (cloud): postgres://user:pass@ep-xxxx.eu-central-1.aws.neon.tech/neondb?sslmode=require
DATABASE_URL=postgres://postgres:postgres@localhost:5432/innov

# ── Auth (email + palavra-passe) ───────────────────────────
# Segredo p/ assinar os JWT de sessão emitidos pelo backend. Gerar aleatório.
JWT_SECRET=troca-isto-por-um-segredo-aleatorio-longo
JWT_EXPIRES_IN=12h

# 1º administrador (seed): email + palavra-passe iniciais para o primeiro login.
SEED_ADMIN_EMAIL=admin@empresa.pt
SEED_ADMIN_PASSWORD=troca-esta-password

# ── Anexos no Google Drive — opcional (senão ficam no disco do servidor) ────
# 1) Service Account JSON (caminho do ficheiro).
GOOGLE_SERVICE_ACCOUNT_FILE=./service-account.json
# 2) ID de um DRIVE PARTILHADO (Shared Drive) onde a service account é membro
#    (Gestor de Conteúdo). Pode ser o Shared Drive ou uma pasta lá dentro.
DRIVE_ROOT_FOLDER_ID=
# Se estas duas estiverem definidas, os anexos vão para o Drive; senão, para
# UPLOADS_DIR (disco). UPLOADS_DIR default: uploads/ (relativo ao server/).
# UPLOADS_DIR=uploads

# ── Notificações ao backoffice ─────────────────────────────
# Opcional: reutiliza o bot Telegram do maganalysis, ou deixa vazio (só in-app).
TELEGRAM_BOT_TOKEN=
TELEGRAM_BACKOFFICE_CHAT_ID=
